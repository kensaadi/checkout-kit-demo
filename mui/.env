# Demo runs fully offline in mock mode — no backend needed.
VITE_PROVIDER=mock
# Dummy values so boot-time config validation passes (unused in mock mode).
VITE_APP_API_HOST=http://localhost:3333
VITE_STRIPE_PK=pk_test_demo
